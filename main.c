#include <stdio.h>
#include <string.h>

int		ft_strlen(char *str);
// char	*ft_strcpy(char *dest, char *src);

int main() {
	// char *str;
	// ft_strcpy(str, "|abdel-ke|");
	// puts(str);
	printf("|%d|\n", ft_strlen("hamid"));
	return 0;
}